# Phase 1–2 (database client + contractor geocoding/seeding)
pandas
requests
supabase
python-dotenv

# Phase 3 (PDF ingestion)
pdfplumber          # text extraction, layout-aware
PyMuPDF             # fitz — embedded photo extraction

# Phase 4 (LLM extraction)
google-genai          # Gemini — fallback provider
requests              # DigitalOcean Gradient inference (OpenAI-compatible REST) — primary
pydantic>=2

# Scanned-PDF fallback: OCR (text) + figure cropping (photos)
pytesseract         # German OCR — NEEDS the Tesseract binary installed separately
Pillow              # page rendering for OCR + cropping
doclayout-yolo      # DocLayout-YOLO figure detection for cropping (pulls torch)
huggingface_hub     # downloads the DocLayout-YOLO weights on first use

# Later phases will add:
#   pyproj              # Phase 5 coordinate conversion
#   numpy               # Phase 5 scoring normalization (log-scaled DTV)
