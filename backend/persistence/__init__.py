"""Phase 6 — persistence: durable writes to Supabase (Postgres + Storage).

  storage.py  upload the PDF + matched photos (deterministic paths → overwrite)
  upsert.py   idempotent bridge upsert + child replace + denormalization
"""
